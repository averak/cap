spring:
  datasource:
    url: jdbc:mysql://localhost:3307/db?useSSL=false&allowPublicKeyRetrieval=true&enabledTLSProtocols=TLSv1.2
    username: db
    password: db
  flyway:
    enabled: true
    clean-disabled: false
